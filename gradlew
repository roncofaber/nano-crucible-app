#!/bin/sh
# Gradle wrapper script placeholder
# Run: gradle wrapper to generate full wrapper
echo "Please run 'gradle wrapper' in Android Studio or with Gradle installed"
echo "Alternatively, open this project in Android Studio and it will auto-generate"
